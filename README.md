# aurora-stores

Pluggable persistence implementations for Aurora and capcompute.

Packages:

- `memory`: in-memory `aurora.Store`, `capcompute.SessionStore`, and `journaled.Journal`.
- `sqlite`: SQLite-backed `aurora.Store`.
